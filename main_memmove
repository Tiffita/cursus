#include <stdio.h>
#include <string.h>

int main() {
    char str[] = "Hello, World!";
    ft_memmove(str + 5, str, 7);  // Moveremos parte de la cadena a sí misma

    printf("Resultado: %s\n", str);

    return 0;
}
